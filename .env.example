# ============================================
# Nihongo Backend - Environment Variables
# ============================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to Git!

# Server Configuration
PORT=8080

# Database Configuration (PostgreSQL)
# For local development:
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/nihongo_db?sslmode=disable
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=your_password_here

# For production (example - Neon/Render PostgreSQL):
# SPRING_DATASOURCE_URL=jdbc:postgresql://your-db-host.neon.tech:5432/nihongo_db?sslmode=require
# SPRING_DATASOURCE_USERNAME=your_db_username
# SPRING_DATASOURCE_PASSWORD=your_db_password

# Email Configuration (Gmail)
# Enable 2FA and create App Password: https://support.google.com/accounts/answer/185833
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_gmail_app_password

# CORS - Frontend Origin
# For local development:
FRONTEND_ORIGIN=http://localhost:3000

# For production (NO trailing slash):
# FRONTEND_ORIGIN=https://your-frontend.vercel.app

# JWT Secret (Base64 encoded, minimum 64 bytes)
# Generate using: bash generate-jwt-secret.sh or generate-jwt-secret.bat
# Or manually: openssl rand -base64 64
JWT_SECRET=REPLACE_WITH_GENERATED_SECRET_MINIMUM_64_BYTES

# ============================================
# Security Reminders:
# ============================================
# 1. Never commit .env to Git (already in .gitignore)
# 2. Use different JWT_SECRET for dev and production
# 3. Keep Gmail app password secure
# 4. Use strong DB password
# 5. FRONTEND_ORIGIN must match exactly (no trailing slash)
# ============================================
